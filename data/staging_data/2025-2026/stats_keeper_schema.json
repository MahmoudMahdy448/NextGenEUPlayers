{
  "rank": "INTEGER",
  "player_name": "VARCHAR(255)",
  "nation": "VARCHAR(255)",
  "position": "VARCHAR(255)",
  "squad": "VARCHAR(255)",
  "competition": "VARCHAR(255)",
  "age": "INTEGER",
  "birth_year": "INTEGER",
  "matches_played": "INTEGER",
  "starts": "INTEGER",
  "minutes": "INTEGER",
  "nineties_played": "DECIMAL(10,2)",
  "goals_against": "DECIMAL(10,2)",
  "goals_against_per_90": "DECIMAL(10,2)",
  "shots_on_target_against": "DECIMAL(10,2)",
  "saves": "DECIMAL(10,2)",
  "save_pct": "DECIMAL(10,2)",
  "wins": "DECIMAL(10,2)",
  "draws": "DECIMAL(10,2)",
  "losses": "DECIMAL(10,2)",
  "clean_sheets": "DECIMAL(10,2)",
  "clean_sheet_pct": "DECIMAL(10,2)",
  "penalty_attempts": "INTEGER",
  "penalty_goals_against": "DECIMAL(10,2)",
  "penalty_saves": "DECIMAL(10,2)",
  "penalty_missed": "DECIMAL(10,2)",
  "MP": "DECIMAL(10,2)",
  "Starts": "DECIMAL(10,2)",
  "Min": "DECIMAL(10,2)",
  "penalty_kicks_allowed": "DECIMAL(10,2)",
  "season": "VARCHAR(50)",
  "table_type": "VARCHAR(50)",
  "processed_at": "TIMESTAMP"
}
{
  "rank": "INTEGER",
  "player_name": "VARCHAR(255)",
  "nation": "VARCHAR(255)",
  "position": "VARCHAR(255)",
  "squad": "VARCHAR(255)",
  "competition": "VARCHAR(255)",
  "age": "INTEGER",
  "birth_year": "INTEGER",
  "nineties_played": "DECIMAL(10,2)",
  "passes_attempted": "DECIMAL(10,2)",
  "passes_live": "DECIMAL(10,2)",
  "passes_dead": "DECIMAL(10,2)",
  "passes_free_kick": "DECIMAL(10,2)",
  "passes_through_ball": "DECIMAL(10,2)",
  "passes_switched": "DECIMAL(10,2)",
  "passes_crosses": "DECIMAL(10,2)",
  "passes_throw_in": "DECIMAL(10,2)",
  "passes_corner_kick": "DECIMAL(10,2)",
  "passes_in": "DECIMAL(10,2)",
  "passes_out": "DECIMAL(10,2)",
  "passes_straight": "DECIMAL(10,2)",
  "passes_completed": "DECIMAL(10,2)",
  "passes_offsides": "DECIMAL(10,2)",
  "passes_blocks": "DECIMAL(10,2)",
  "season": "VARCHAR(50)",
  "table_type": "VARCHAR(50)",
  "processed_at": "TIMESTAMP"
}
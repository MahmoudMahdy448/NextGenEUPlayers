{
  "rank": "INTEGER",
  "player_name": "VARCHAR(255)",
  "nation": "VARCHAR(255)",
  "position": "VARCHAR(255)",
  "squad": "VARCHAR(255)",
  "competition": "VARCHAR(255)",
  "age": "INTEGER",
  "birth_year": "INTEGER",
  "matches_played": "INTEGER",
  "minutes": "INTEGER",
  "minutes_per_match": "DECIMAL(10,2)",
  "minutes_pct": "DECIMAL(10,2)",
  "nineties_played": "DECIMAL(10,2)",
  "starts": "INTEGER",
  "minutes_per_start": "DECIMAL(10,2)",
  "complete_matches": "DECIMAL(10,2)",
  "substitutions": "DECIMAL(10,2)",
  "minutes_per_sub": "DECIMAL(10,2)",
  "unused_sub": "DECIMAL(10,2)",
  "points_per_match": "DECIMAL(10,2)",
  "on_goals": "DECIMAL(10,2)",
  "on_goals_against": "DECIMAL(10,2)",
  "plus_minus": "DECIMAL(10,2)",
  "plus_minus_per_90": "DECIMAL(10,2)",
  "on_off": "DECIMAL(10,2)",
  "on_expected_goals": "DECIMAL(10,2)",
  "on_expected_goals_against": "DECIMAL(10,2)",
  "expected_goals_plus_minus": "DECIMAL(10,2)",
  "expected_goals_plus_minus_per_90": "DECIMAL(10,2)",
  "MP": "DECIMAL(10,2)",
  "Min": "DECIMAL(10,2)",
  "Starts": "DECIMAL(10,2)",
  "season": "VARCHAR(50)",
  "table_type": "VARCHAR(50)",
  "processed_at": "TIMESTAMP"
}